version: "2"
linters:
  default: standard
  disable:
    # LSP notifications and stdout writes are intentionally best-effort.
    - errcheck
  exclusions:
    generated: lax
    rules:
      # The Typst plugin uses unsafe pointer arithmetic for the wasm ABI.
      - path: typst/plugin/
        linters: [govet]
